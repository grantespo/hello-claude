# hello-claude

A minimal Node.js Hello World project for learning Claude Code.

## Running the project

Always use this command to test:

```bash
node index.js
```

## Guidelines

- Keep `index.js` simple and readable
- If asked to add a feature, implement it in `index.js` unless it warrants a new file
- After any change, verify it works by running `node index.js`
